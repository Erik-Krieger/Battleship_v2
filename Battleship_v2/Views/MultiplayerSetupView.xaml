<UserControl x:Class="Battleship_v2.Views.MultiplayerSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battleship_v2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height=".5*"/>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height=".5*"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="36" Text="Multiplayer" />
        <Button Grid.Column="1" Grid.Row="2" Margin="8" Content="Host" Command="{Binding CmdHost}"/>
        <Button Grid.Column="1" Grid.Row="3" Margin="8" Content="Join" Command="{Binding CmdJoin}"/>
        <Button Grid.Column="1" Grid.Row="4" Margin="8" Content="Menu" Command="{Binding CmdOpenMainMenu}"/>
    </Grid>
</UserControl>
