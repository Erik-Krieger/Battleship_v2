<UserControl x:Class="Battleship_v2.Views.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battleship_v2.Views"
             xmlns:vm="clr-namespace:Battleship_v2.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.DataContext>
            <vm:MainMenuViewModel />
        </Grid.DataContext>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Column="1" Grid.Row="1" Margin="10" Content="Singleplayer"/>
        <Button Grid.Column="1" Grid.Row="2" Margin="10" Content="Multiplayer"/>
        <Button Grid.Column="1" Grid.Row="3" Margin="10" Content="Exit" Click="CloseButtonClicked"/>
    </Grid>
</UserControl>
