<UserControl x:Class="Battleship_v2.Views.AudioControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:Battleship_v2.ViewModels"
             xmlns:local="clr-namespace:Battleship_v2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid MaxHeight="200" MaxWidth="300" Visibility="{Binding WindowVisibility}">
        <Grid.InputBindings>
            <KeyBinding Key="Enter" Command="{Binding CmdPButtonPressed}" />
        </Grid.InputBindings>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Row="1" Margin="10" Content="Sounds" Command="" />
        <Button Grid.Row="2" Margin="10" Content="Music" Command="" />
    </Grid>
</UserControl>
